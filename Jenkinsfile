pipeline {
    agent any

    stages {
        stage('Afficher la date') {
            steps {
                script {
                    sh 'date'
                }
            }
        }
    }
}
